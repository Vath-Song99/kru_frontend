"use client";

import React, {FC, ReactNode} from "react";

interface BlockFooterProps {
    children: React.ReactNode;
    className?: string;
}

const BlockFooter: FC<BlockFooterProps> = ({
    children,
    className,
})

